apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: zerofiltretech-dev
  name: zerofiltretech-blog-front-dev
spec:
  minReadySeconds: 30
  progressDeadlineSeconds: 120
  replicas: 1
  selector:
    matchLabels:
      app: zerofiltretech-blog-front-dev
  template:
    metadata:
      labels:
        app: zerofiltretech-blog-front-dev
    spec:
      containers:
        - name: zerofiltretech-blog-front-dev
          image: imzerofiltre/zerofiltretech-blog-front:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  namespace: zerofiltretech-dev
  name: zerofiltretech-blog-front-dev
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: zerofiltretech-blog-front-dev
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: zerofiltretech-dev
  name: zerofiltretech-blog-front-dev
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    # add an annotation indicating the issuer to use.
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: "blog-dev.zerofiltre.tech"
      http:
        paths:
          - path: /
            backend:
              serviceName: zerofiltretech-blog-front-dev
              servicePort: 80
  tls: # < placing a host in the TLS config will indicate a certificate should be created
    - hosts:
        - "blog-dev.zerofiltre.tech"
      secretName: blog-front-zerofiltre-ingress-cert # < cert-manager will store the created certificate in this secret.
